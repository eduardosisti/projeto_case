[pytest]
# Configurações para o pytest

# Define o diretório de testes, se não estiver na raiz
testpaths = tests

# Configurações específicas para plugins
markers =
    slow: mark test as slow
    network: mark test as requiring network
